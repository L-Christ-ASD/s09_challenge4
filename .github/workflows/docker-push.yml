
---

name: docker push

on:
  workflow_dispatch:
  push:


env:
  REGISTRY: docker.io
  IMAGE_NAME: christasd/myapp-cowsay-go
    

jobs:
  login:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout-file
        uses: actions/checkout@v4

      - name: Verify context files
        run: ls -al ../..

      - name: Verify curent file
        run: pwd


      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          tags: |

            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
      
      - name: Print Docker tags 
        run: echo "${{ steps.meta.outputs.tags }}"
        
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.COWSAY_GO_USR }}
          password: ${{ secrets.COWSAY_GO_MD }}

      - name: Docker info 
        run: docker info

      - name: whoami
        run: whoami

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context:  /home/runner/work/s09_challenge4/s09_challenge4/
          push: true
          tags: "${{ steps.meta.outputs.tags }}"
          labels: "${{ steps.meta.outputs.labels }}"
          
